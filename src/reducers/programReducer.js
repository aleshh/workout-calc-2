const initialState = [
  {
    id: 0,
    exercises: [0, 1, 2]
  },
  {
    id: 1,
    exercises: [0, 3, 4]
  }
]

export default (state = initialState, action) => {
  switch (action) {
    default:
      return state
  }
}
